package common

const (
	// ServiceWeb service name
	ServiceWeb = "web"

	// ServiceCronnie service name
	ServiceCronnie = "cronnie"

	// ServiceTasker service name
	ServiceTasker = "tasker"
)

const (
	// QueueMain the queue
	QueueMain = "queue_main"
)

const (
	// TypeParser message type for parser
	TypeParser = "parser"
)
